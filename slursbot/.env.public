# slurs.tf
SLURS_API_KEY=
SLURS_COOKIES=

#SLURS_API_KEY=PUT_KEY_IF_REQUIRED  


OZF_MAX_IDS=


SLURS_API_BASE=https://slurs.tf
SLURS_CATEGORY=total           # what we want; the module will fallback automatically on 500
SLURS_BATCH_SIZE=10            # per slurs.tf guidance (≤10 steamids per call)
SLURS_LIMIT=100
SLURS_SLEEP_MS=500
SLURS_RETRIES_S=10,30,300,900

# Fallback lexicon (used only when category call 500s)


REPORTS_DIR=C:/slurs/reports



DISCORD_PUBLIC_COLOR=A7C7E7



# Roster refresh pacing
OZF_REFRESH_PROBE=300            # max pages to probe forward each day
OZF_REFRESH_404_STREAK=20        # stop after this many consecutive 404s
OZF_REFRESH_SLEEP_MS=200         # polite delay per page
DISPLAY_TZ=Australia/Adelaide    # used for the 22:00 local-day window

LOG_LEVEL=DEBUG
OVERLAP_HOURS=72

OZF_MAX_PROBE=20
OZF_STOP_AFTER_404=false


ALLOWLIST_DROP=1
ALLOWLIST_PATH=C:\slurs\slursbot\allowlist.yaml
LEXICON_PATH=C:\slurs\slursbot\lexicon.yaml
